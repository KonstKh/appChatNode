extends layout

block title
	title interpolation title

block content
	h1 Hello from jade moin
	table.table.table-striped
		thead
			tr
				th Name
				th ID
		tbody
			each room in rooms
				tr
					td=room.name
					td=room.id

	//= JSON.stringify(rooms)
	//ul
	//each val in JSON.stringify(rooms)
	//	li=val
